// tests/kernel/drivers/vello/hw_smoke_g4_placeholder.pdx --
// G4.M4-003 (#1490)
//
// D7-dormant HW-smoke placeholder for the "draw complex vector
// scene on real T14 (Ghostscript tiger)" acceptance. The recipe
// lives in tools/hw-smoke-g4.md; this module compiles and links but
// is NOT registered in the QEMU boot witness path per gated:hardware
// discipline.
//
// The real HW-smoke opens a KIND_VELLO_RENDERER against the T14
// Iris Xe GPU context, encodes the Ghostscript tiger (a hardcoded
// 2000+-path SVG-lineage vector scene) through vello_render_channel,
// submits it, and asserts the CPU-fallback hash matches within a
// 100 ms watchdog against the pre-recorded golden hash.

module HwSmokeG4Placeholder = structure {

  pub let hw_smoke_g4_stub : () -> () !{} @{} =
    fn () -> unsafe {
      effects: {}, capabilities: {},
      justification: "G4.M4-003 (#1490): dormant HW-smoke stub. Not registered under QEMU; see tools/hw-smoke-g4.md for the real T14 recipe.",
      block: {
        ret
      }
    }
}
