// r10-m1-001: probe for push rax; push r15
module Preflightpush = structure {
  let probe : () -> () !{} @{} = fn (x: ()) -> unsafe {
    effects: {}, capabilities: {},
    justification: "r10-m1-001 encoder verification probe",
    block: {
      push rax;
      push r15;
      ret
    }
  }
}
