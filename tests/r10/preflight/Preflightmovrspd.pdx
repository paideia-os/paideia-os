// r10-m1-001: probe for mov [rsp + 8], rax (SIB encoding, PA-R10-001)
module Preflightmovrspd = structure {
  let probe : () -> () !{} @{} = fn (x: ()) -> unsafe {
    effects: {}, capabilities: {},
    justification: "r10-m1-001 encoder verification probe for SIB",
    block: {
      mov [rsp + 8], rax;
      ret
    }
  }
}
