// r10-m1-001: probe for mov [rbp], rax (BP escape, PA-R10-001)
module Preflightmovbpd = structure {
  let probe : () -> () !{} @{} = fn (x: ()) -> unsafe {
    effects: {}, capabilities: {},
    justification: "r10-m1-001 encoder verification probe for BP escape",
    block: {
      mov [rbp], rax;
      ret
    }
  }
}
