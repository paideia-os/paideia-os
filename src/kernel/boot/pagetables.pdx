// src/kernel/boot/pagetables.pdx — B2-006: Superseded by tools/boot_stub.S
// Stub retained for compile-time compatibility.
// Page-table setup now handled in 32-bit assembly (tools/boot_stub.S) before _start entry.

module Pagetables = structure {
  // Stub — page tables now in tools/boot_stub.S (.bss section)
}
