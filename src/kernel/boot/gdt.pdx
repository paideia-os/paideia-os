// src/kernel/boot/gdt.pdx — B2-006: Superseded by tools/boot_stub.S
// Stub retained for compile-time compatibility.
// GDT setup now handled in 32-bit assembly (tools/boot_stub.S) before _start entry.

module Gdt = structure {
  // Stub — GDT now in tools/boot_stub.S
}
